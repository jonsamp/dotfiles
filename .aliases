#!/usr/bin/env zsh

# Recursively delete `.DS_Store` files
alias cleanup="find . -type f -name '*.DS_Store' -ls -delete"

# Reload the shell (i.e. invoke as a login shell)
alias reload="source ~/.zshrc"

# hub
alias git=hub

alias code="code ."
alias dev="cd ~/dev/"
alias devexpo="cd ~/dev/expo/"
alias master="git checkout master"
alias force="git push --force"
alias rebase="git rebase -i master"
alias save_dotfiles="cd ~/Dropbox/Development/dotfiles/ && sh update_dotfiles.sh"

alias expod="~/dev/expo/expo-cli/packages/expo-cli/bin/expo.js"
alias universe="~/dev/expo/universe/"
alias www="~/dev/expo/universe/server/www/"
alias website="~/dev/expo/universe/server/website"
alias snack="~/dev/universe/server/snack"
alias docs="~/dev/expo/expo/docs"